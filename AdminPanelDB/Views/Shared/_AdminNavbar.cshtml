<nav class="admin-navbar">
    <a asp-controller="Admin" asp-action="AbRef">Abteilungen & Referate</a>
    <a asp-controller="Admin" asp-action="Personen">Personen</a>
    <a asp-controller="Admin" asp-action="Config">Config</a>
</nav>
<hr />

<style>
    /* Navbar для админки */
    .admin-navbar {
        background-color: #1f1f1f; /* темный фон, как у header */
        padding: 10px 20px;
        border-radius: 6px;
        display: flex;
        gap: 20px;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        font-size: 14px;
        margin-bottom: 15px;
    }

        .admin-navbar a {
            color: #e0e0e0; /* светлый текст */
            text-decoration: none;
            font-weight: 400;
            padding: 6px 12px;
            border-radius: 4px;
            transition: background 0.3s ease, color 0.3s ease;
        }

            .admin-navbar a:hover {
                background-color: #2a2a2a; /* чуть светлее при наведении */
                color: #ffffff;
            }

            .admin-navbar a.active {
                background-color: #333333; /* активная ссылка */
                color: #ffffff;
            }

</style>
